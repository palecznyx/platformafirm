<p id="notice"><%= notice %></p>

<div class="panel panel-default">
  <div class="panel-heading"><h2>Lista projektow</h2></div>
  <div class="panel-body">
    
<table>
  <thead>
    <tr>
      <th>Obraz</th>
      <th>Opis</th>
      <th colspan="3"></th>
    </tr>
    
    <div id="pins" class="transitions-enabled">
    <% @pins.each do |pin| %>
    <div class="box"></div>
    <div class="panel panel-default">
    <%= link_to image_tag(pin.image.url(:medium)),pin %><br/>
    <div class="panel-body">
      
        <%= pin.description %><br/>
        <div class "panel-footer">
        <%= link_to 'Edycja', edit_pin_path(pin) %><br/>
        <%= link_to 'Usun', pin, method: :delete, data: { confirm: 'Jesetes pewien?' } %><br/>
        
      </div>
    
</div>
</div>
<% end %>

<br>

<%= link_to 'Nowe zdjecie/budowa', new_pin_path %>

<%= will_paginate @posts %>
